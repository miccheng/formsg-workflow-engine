{
  "name": "workflows",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/workflows/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/workflows",
        "tsConfig": "apps/workflows/tsconfig.lib.json",
        "packageJson": "apps/workflows/package.json",
        "main": "apps/workflows/src/index.ts",
        "assets": ["apps/workflows/*.md"]
      }
    }
  }
}
